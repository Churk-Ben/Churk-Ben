import { icon } from '@fortawesome/fontawesome-svg-core'
import { faGithub, faBilibili } from '@fortawesome/free-brands-svg-icons'
import { faLink } from '@fortawesome/free-solid-svg-icons'

export const githubSvg = icon(faGithub).html.join('')
export const bilibiliSvg = icon(faBilibili).html.join('')
export const linkSvg = icon(faLink).html.join('')
